<?php
namespace Draeli\RssBridge;

$arrConfig = array(
    // Define configuration for each bridge
    'bridge' => array(
        'LeBonCoin' => array(
            'displayImage' => true, // (boolean) Define if result must display or not product image
            'numberOfPages' => 1, // (integer greater than 0) Actually better idea don't touch
        ),
    ),
    'format' => array(
        
    ),
);
